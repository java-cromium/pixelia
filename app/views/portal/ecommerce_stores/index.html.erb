<% content_for(:title) { "E-Commerce Stores — Pixelia" } %>

<div class="p-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-brand-text dark:text-slate-50">E-Commerce Stores</h1>
    <p class="mt-1 text-brand-text-light dark:text-slate-400">Connected stores and their sync status.</p>
  </div>

  <% if @ecommerce_stores.any? %>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @ecommerce_stores.each do |store| %>
        <a href="<%= portal_ecommerce_store_path(store) %>" class="block bg-brand-card dark:bg-slate-800/50 rounded-2xl border border-brand-border dark:border-slate-700/50 p-6 hover:border-brand-highlight/50 hover:shadow-lg hover:shadow-brand-highlight/5 transition-all duration-200">
          <div class="flex items-center gap-3 mb-4">
            <div class="w-10 h-10 bg-brand-highlight/15 rounded-lg flex items-center justify-center">
              <svg class="w-5 h-5 text-brand-highlight dark:text-indigo-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/></svg>
            </div>
            <div>
              <h3 class="font-bold text-brand-text dark:text-slate-50"><%= store.platform.capitalize %></h3>
              <p class="text-xs text-brand-text-light dark:text-slate-500"><%= store.store_url %></p>
            </div>
          </div>
          <div class="pt-4 border-t border-brand-border dark:border-slate-700/50">
            <span class="text-xs font-medium px-2.5 py-1 rounded-full <%= store.connected? ? 'bg-emerald-500/15 text-emerald-600 dark:text-emerald-400' : 'bg-amber-500/15 text-amber-600 dark:text-amber-400' %>">
              <%= store.connected? ? "Connected" : "Keys Missing" %>
            </span>
          </div>
        </a>
      <% end %>
    </div>
  <% else %>
    <div class="bg-brand-card dark:bg-slate-800/50 rounded-2xl border border-brand-border dark:border-slate-700/50 p-16 text-center">
      <div class="w-16 h-16 bg-brand-highlight/15 rounded-full flex items-center justify-center mx-auto mb-4">
        <svg class="w-8 h-8 text-brand-highlight dark:text-indigo-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/></svg>
      </div>
      <h3 class="text-lg font-semibold text-brand-text dark:text-slate-50">No stores connected</h3>
      <p class="mt-2 text-brand-text-light dark:text-slate-500 text-sm">E-commerce store integrations will appear here once configured.</p>
    </div>
  <% end %>
</div>
