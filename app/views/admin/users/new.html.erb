<% content_for(:title) { "New User — Admin — Pixelia" } %>

<div class="p-8">
  <div class="mb-6">
    <a href="<%= admin_users_path %>" class="text-sm text-brand-highlight dark:text-cyan-400 hover:text-brand-highlight-hover dark:hover:text-cyan-300 font-medium">&larr; Back to Users</a>
  </div>

  <div class="max-w-xl">
    <h1 class="text-3xl font-bold text-brand-text dark:text-slate-50 mb-8">New User</h1>
    <div class="bg-brand-card dark:bg-slate-800/50 rounded-2xl border border-brand-border dark:border-slate-700/50 p-8">
      <%= render "form", user: @user %>
    </div>
  </div>
</div>
