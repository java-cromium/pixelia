<% content_for(:title) { "New Store — Admin — Pixelia" } %>

<div class="p-8">
  <div class="mb-6">
    <a href="<%= admin_ecommerce_stores_path %>" class="text-sm text-cyan-400 hover:text-cyan-300 font-medium">&larr; Back to Stores</a>
  </div>

  <div class="max-w-xl">
    <h1 class="text-3xl font-bold text-slate-50 mb-8">New E-Commerce Store</h1>
    <div class="bg-slate-800/50 rounded-2xl border border-slate-700/50 p-8">
      <%= render "form", store: @store %>
    </div>
  </div>
</div>
